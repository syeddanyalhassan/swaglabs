{
  "name": "cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "ui-regression-allure": "cypress run --spec cypress/integration/testsuites/**.js --env allure=true",
    "allure-report":"allure generate allure-results --clean -o allure-report"
  },
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.34.0",
    "cypress": "^11.0.1"
  }
}
